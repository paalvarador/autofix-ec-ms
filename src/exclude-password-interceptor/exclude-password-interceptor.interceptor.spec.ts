import { ExcludePasswordInterceptor } from './exclude-password-interceptor.interceptor';

describe('ExcludePasswordInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ExcludePasswordInterceptor()).toBeDefined();
  });
});
